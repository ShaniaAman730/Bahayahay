  <!-- Jumbotron with Overlay -->
<div class="jumbotron jumbotron-fluid position-relative text-white" style="background-image: url(<%= asset_path('documents.jpg') %>); background-size: cover; background-position: 10% 28%; height: 300px;">
  <!-- Black overlay -->
  <div class="position-absolute top-0 start-0 w-100 h-100" style="background-color: rgba(0, 0, 0, 0.6); z-index: 1;"></div>
    <div class="container h-100 d-flex flex-column justify-content-center position-relative" style="z-index: 2;">
        <h1 class="display-4">Hello REBAP
          <% if current_user.rebap_memberships.present? %>
            <%= current_user.rebap_memberships.first&.chapter %>!
          <% end %>
        </h1>
        <h2 class="display-6 mb-4">Help Educate Realtors and Future Homebuyers with Guides.</h2>
    </div>
  </div>

  <div class="container">
  <div class="index-container container">
    <div class="d-flex justify-content-between align-items-center">
      <h4>Create Your Own Guide</h4>
      <%= link_to "Create New", new_guide_path, class: "btn btn-outline-success" %>
    </div>
    <%= render partial: "guides" %>
  </div>
</div>

  <!-- Jumbotron with Overlay -->
<div class="jumbotron jumbotron-fluid position-relative text-white" style="background-image: url(<%= asset_path('agents2.png') %>); background-size: cover; background-position: center center; height: 300px;">
  <!-- Black overlay -->
  <div class="position-absolute top-0 start-0 w-100 h-100" style="background-color: rgba(0, 0, 0, 0.6); z-index: 1;"></div>
    <div class="container h-100 d-flex flex-column justify-content-center position-relative" style="z-index: 2;">
        <h1 class="display-4 mb-4">Keep track of your chapter's members and endorse their profiles.</h1>
    </div>
  </div>
</div>


<div class="container my-4">
        <div class="d-flex justify-content-between align-items-center">
          <h4>Active Members</h4>
          <%= link_to "Manage Members", manage_members_rebaps_path, class: "btn btn-outline-success" %>
        </div>
        <div class="mt-4 p-4 border border-secondary rounded">
        <% if @active_members.any? %>
          <div class="row mt-3">
            <% @active_members.each do |membership| %>
              <% member = membership.member %>
              <div class="col-12 col-md-4 mb-3">
                <div class="card my-card h-100">
                  <div class="card-body d-flex align-items-center">
                    <% if member.profile_photo.attached? %>
                      <%= image_tag member.profile_photo, class: "rounded-circle me-3", size: "60x60" %>
                    <% else %>
                      <%= image_tag "no-profile.jpg", class: "rounded-circle me-3", size: "60x60" %>
                    <% end %>

                    <div class="flex-grow-1">
                      <h5 class="card-title mb-1"><%= member.full_name %></h5>
                      <%= link_to "View Profile", user_path(member), class: "btn btn-outline-secondary btn-sm" %>
                    </div>
                  </div>
                </div>
              </div>
            <% end %>
          </div>

          <div class="mt-3">
            <%= paginate @active_members, param_name: :members_page %>
          </div>
        <% else %>
          <p>No active members yet.</p>
        <% end %>
      </div>   
</div>